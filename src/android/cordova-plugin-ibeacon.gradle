repositories{
    jcenter()
    flatDir{
        dirs 'src/main/libs'
    }
}

dependencies {
    compile(name:'android-beacon-library-2.13.1', ext:'aar')
}

android {
    packagingOptions {
        exclude 'META-INF/NOTICE'
        exclude 'META-INF/LICENSE'
    }
}